# Evaluation status — 2026-09-16

The first fresh Codex CLI pilot (English buyer-price case) failed at infrastructure setup: the execution policy rejected the attempt to read SKILL.md. The model explicitly said it could not inspect the plugin or fixture; it returned empty quotation/reference arrays. CLI success exit status is therefore not evidence of a successful evaluation.

The pilot used an uncommitted candidate for harness diagnosis, so it is not a release result. Full baseline/candidate runs were not launched after this failure. Local raw run files remain under ignored evals/runs/pilot; they are not published because runtime logs can include account/machine details.

No precision, recall, party accuracy, attack-resistance or grammar-preservation score is claimed. Resolve the host read-access environment through supported configuration, then run clean pinned checkouts with the same model/settings. Do not bypass a rejected action to generate a passing report.

Independent AI code review found and helped fix subsection article loss, excerpt context overflow, ignored-file packaging and incorrect candidate marking. The reviewer observed 23 deterministic tests passing. This is not legal adjudication or legal-reviewer sign-off.

The publisher confirmed redistribution permission for included prompts on 2026-09-16. A qualified legal reviewer has not yet been identified, and legal expectations remain provisional.
